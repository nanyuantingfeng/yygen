{
  "version": "1.0.0-beta.0",
  "description": "this is an yygen template paoject!!",
  "license": "MIT",
  "main": "./lib/index.js",
  "module": "./esm/index.js",
  "types": "./esm/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "build": "npm run optimize && npm run copy",
    "babel:cjs": "babel src --out-dir dist/lib --plugins=lodash,@babel/plugin-transform-modules-commonjs",
    "babel:mjs": "babel src --out-dir dist/esm --plugins=lodash",
    "copy": "cp package.json README.md .npmignore dist",
    "optimize": "npm run babel:cjs && npm run babel:mjs",
    "publish2npm": "npm run build && npm publish dist"
  },
  "devDependencies": {
    "hollow-cli": "^1.3.7",
    "prettier": "^1.14.2"
  }
}
