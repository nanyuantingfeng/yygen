{
  "version": "1.0.0-beta.0",
  "description": "this is an yygen template paoject!!",
  "license": "MIT",
  "main": "./lib/index.js",
  "module": "./esm/index.js",
  "types": "./esm/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "build": "npm run optimize",
    "optimize": "npm run babel:cjs",
    "babel:cjs": "babel src --out-dir dist/lib",
    "publish2npm": "npm run build && cp package.json README.md .npmignore dist && npm publish dist"
  },
  "devDependencies": {
    "hollow-cli": "^1.1.0-beta.12",
  }
}
