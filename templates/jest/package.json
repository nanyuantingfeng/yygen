{
  "scripts": {
    "test:watch": "jest -o --watch",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^24",
    "jest": "^24",
    "ts-jest": "^24",
    "babel-jest": "^24"
  }
}
